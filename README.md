# scoop-bucket

A [Scoop](https://scoop.sh) bucket for randlee packages (sc-compose, atm, sc-lint, scmux, etc.).

## Adding this bucket

```powershell
scoop bucket add randlee https://github.com/randlee/scoop-bucket
```

## Layout

Package manifests (JSON files) live under [`bucket/`](./bucket).
